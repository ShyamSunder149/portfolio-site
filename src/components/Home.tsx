import { headingCss } from "../utils/tailwindVars";

const Home: React.FC = () => {
  return (
    <div className="">
      <h2 className={`${headingCss}`}>Konnichiwa, Nitro here!!</h2>
      Developer, Interested in creating minimalistic customised stuffs
    </div>
  );
};

export default Home;
